// angular.module("dataPortalApp").run(function ($rootScope, $state, $transitions ) {
//      $transitions.onSuccess({to:'*'}, function ($state) {
//         // do stuff on every transition such as change page title [global scope here]
//      }
// }
